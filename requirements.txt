# Dependências para o projeto de Classificação de Retinopatia Diabética

# Kaggle API
kagglehub>=0.3.0

# Machine Learning e Deep Learning
# Nota: TensorFlow não tem suporte oficial para Python 3.14 ainda
# Alternativa: usar tf-nightly ou usar Python 3.11
tensorflow-macos>=2.13.0; platform_system == 'Darwin' and platform_machine == 'arm64'
tensorflow>=2.13.0; platform_system != 'Darwin' or platform_machine != 'arm64'
scikit-learn>=1.3.0

# Processamento de imagens
opencv-python>=4.8.0
Pillow>=10.0.0

# Visualização
matplotlib>=3.7.0
seaborn>=0.12.0

# Manipulação de dados
numpy<2.0.0
pandas>=2.0.0

# Utilitários
tqdm>=4.65.0

# Jupyter para executar notebooks
jupyter>=1.0.0
ipykernel>=6.25.0
